# LifeHub env vars (all optional — app runs fully offline without any of these)
#
# Sync (paste these in Settings → Sync instead of here if you prefer per-device config)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
#
# Phase 2 — AI-assisted capture classifier
# OPENAI_API_KEY=
#
# Phase 4 integrations (configured per-user via Settings UI; these are server-side fallbacks)
# AIOS_BASE_URL=http://localhost:8080
# AIOS_API_KEY=
# APPBLUEPRINTS_DATA_DIR=/Users/you/Devoloper Projects/AppBlueprints/data/mcps
# APPMAKER_BASE_URL=http://localhost:5000
# COSMO_BASE_URL=http://localhost:4000
